{
  "name": "squad-sentinel",
  "private": true,
  "description": "Monorepo: app lives in frontend/. Install with npm --prefix frontend install; Vercel uses vercel.json.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
